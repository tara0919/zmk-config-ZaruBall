description: Track layer activity and restore the IME when the layer exits

compatible: "zmk,input-processor-ime-activity"

include: ip_zero_param.yaml

properties:
  layer:
    type: int
    required: true

  enter-keycode:
    type: int
    required: true

  exit-keycode:
    type: int
    required: true
