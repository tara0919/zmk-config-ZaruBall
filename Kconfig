# Copyright (c) 2026 The ZMK Contributors
# SPDX-License-Identifier: MIT

config ZARUBALL_BATTERY_TEXT
    bool "Type both ZaruBall battery levels"
    default y
    depends on SHIELD_ZARUBALL_RIGHT
    depends on ZMK_BATTERY_REPORTING
    depends on ZMK_SPLIT_ROLE_CENTRAL
    depends on ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING

config ZARUBALL_INERTIAL_SCROLL
    bool "ZaruBall inertial scroll input processor"
    default y
    depends on SHIELD_ZARUBALL_RIGHT
    depends on ZMK_POINTING
    depends on ZMK_SPLIT_ROLE_CENTRAL
    depends on INPUT

config ZARUBALL_INERTIAL_SCROLL_DEBUG
    bool "Log ZaruBall inertial scroll input windows"
    default n
    depends on ZARUBALL_INERTIAL_SCROLL

config ZARUBALL_AXIS_KEYS
    bool "ZaruBall axis-to-key input processor"
    default y
    depends on SHIELD_ZARUBALL_RIGHT
    depends on ZMK_POINTING
    depends on ZMK_SPLIT_ROLE_CENTRAL
    depends on INPUT

config ZARUBALL_AXIS_KEYS_THREAD_STACK_SIZE
    int "ZaruBall axis-to-key worker thread stack size"
    default 1536
    depends on ZARUBALL_AXIS_KEYS

config ZARUBALL_AXIS_KEYS_THREAD_PRIORITY
    int "ZaruBall axis-to-key worker thread priority"
    default 8
    depends on ZARUBALL_AXIS_KEYS
